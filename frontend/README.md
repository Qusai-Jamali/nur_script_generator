# NUR Frontend (React + Vite)

## Run locally

1. Copy `.env.example` to `.env`
2. Set `VITE_API_BASE_URL` to backend URL (e.g. `http://localhost:8000`)
3. Install and run:

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
